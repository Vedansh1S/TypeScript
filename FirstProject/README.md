# FirstProject

Small sandbox for experimenting with TypeScript basics (types, interfaces, callbacks, async helpers).

## Usage

```bash
npm install
npm install -g typescript
tsc
node dist/index.js
# or
npx ts-node src/index.ts
```

Source lives in `src/`, compiled files go to `dist/` (ignored by git).
