# OTP Generator

A React + TypeScript application that generates secure one-time passwords with customizable length and alphanumeric options.

## How to Run

```bash
npm install
npm run dev
```

Open the URL shown in the terminal (usually `http://localhost:5173`).

